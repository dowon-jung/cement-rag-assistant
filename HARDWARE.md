# 🖥 하드웨어 스펙 가이드

> 시멘트 제조업 특화 Hybrid RAG 어시스턴트 운영에 필요한 하드웨어 요구사항

---

## 1. 운영 환경별 구성 요약

| 환경 | CPU | RAM | 디스크 | GPU | 비고 |
|------|-----|-----|--------|-----|------|
| **로컬 개발** | 8코어 | 32GB | 500GB SSD | 불필요 | Ollama CPU + Anthropic API |
| **사내 소규모** | 16코어 | 64GB | 1TB NVMe | 불필요 | Ollama CPU, 동시 5~10명 |
| **온프레미스 GPU** | 16코어 | 64GB | 1TB NVMe | RTX 4090 (24GB) | vLLM, 동시 20~50명 |
| **고객사 납품 (폐쇄망)** | 서버 2대 분리 | 아래 참고 | 아래 참고 | RTX 4090 | 완전 에어갭 |

---

## 2. 구성 요소별 리소스 요구사항

### FastAPI 앱 서버

| 항목 | 최소 | 권장 | 비고 |
|------|------|------|------|
| CPU | 2코어 | 4코어 | async 처리라 코어 수보다 동시성이 중요 |
| RAM | 2GB | 4GB | |
| 디스크 | 10GB | 20GB | 로그 포함 |
| 네트워크 | 100Mbps | 1Gbps | SSE 스트리밍 연결 유지 필요 |

---

### PostgreSQL

| 항목 | 최소 | 권장 | 비고 |
|------|------|------|------|
| CPU | 2코어 | 4코어 | |
| RAM | 4GB | 16GB | shared_buffers = RAM의 25% 권장 |
| 디스크 | 50GB SSD | 200GB SSD | ERP 2년치 + 캐시 영구화 |
| IOPS | 1,000 | 5,000 | 배치 수집 시 쓰기 집중 |

```
예상 데이터 크기:
  production_logs (2년치): 약 730,000행 → ~500MB
  news_cache:              약 100,000건 → ~200MB
  market_rates:            약 2,000행   → 소량
  총합 (여유 포함):         ~50GB
```

---

### Redis

| 항목 | 최소 | 권장 | 비고 |
|------|------|------|------|
| CPU | 1코어 | 2코어 | 싱글 스레드 구조라 코어 수 영향 적음 |
| RAM | 2GB | 8GB | TTL 캐시 + 임베딩 캐시 포함 |
| 디스크 | 5GB | 20GB | AOF 영속화 사용 시 |

```
예상 메모리 사용:
  환율/뉴스/날씨 캐시:                      ~100MB
  임베딩 캐시 (768 dim × 10,000건):          ~30MB
  검색 결과 캐시:                            ~200MB
  총합:                                      ~500MB → 2GB면 충분
```

---

### Qdrant

| 항목 | 최소 | 권장 | 비고 |
|------|------|------|------|
| CPU | 2코어 | 8코어 | 벡터 검색 병렬화 |
| RAM | 8GB | 32GB | 인덱스 메모리 적재 권장 |
| 디스크 | 50GB SSD | 200GB SSD | 벡터 + payload 저장 |

```
예상 벡터 크기:
  768 dim × float32 = 약 3KB/벡터
  규제 문서 500청크:       ~1.5MB
  유연탄 CSV 200청크:      ~0.6MB
  매뉴얼/품질기준 1,000청크: ~3MB
  총 초기 벡터:            ~2,000개 → 약 10MB
  → 초기에는 소량, 문서 추가 시 선형 증가
```

---

### Neo4j

| 항목 | 최소 | 권장 | 비고 |
|------|------|------|------|
| CPU | 2코어 | 4코어 | |
| RAM | 4GB | 16GB | 그래프 탐색 캐시 |
| 디스크 | 20GB SSD | 50GB SSD | |

```
예상 그래프 크기:
  노드 약 1,000개, 엣지 약 3,000개 → ~100MB
  → Neo4j Community Edition으로 충분
```

---

### Kafka (KRaft 모드)

| 항목 | 단일 노드 | 3노드 클러스터 | 비고 |
|------|-----------|----------------|------|
| CPU | 2코어 | 4코어/노드 | |
| RAM | 4GB | 8GB/노드 | JVM 힙 2~4GB 권장 |
| 디스크 | 100GB | 500GB/노드 | 메시지 로그 보존 최대 30일 |

```
예상 메시지 볼륨:
  일 수집 이벤트:  ~100건
  인덱싱 요청:     ~10건/일
  LLM 요청:        ~50건/일
  → 소규모 트래픽, 단일 노드도 충분
    단, 장애 격리를 위해 3노드 권장
```

---

### LLM 서빙 — Ollama (CPU 전용)

| 모델 | RAM 요구량 | 응답 속도 (CPU) | 적합한 상황 |
|------|-----------|-----------------|-------------|
| Gemma2:2b | 4GB | ~5 tok/s | 테스트, 데모 |
| Gemma2:9b | 16GB | ~2 tok/s | 소규모 운영 |
| Gemma2:27b | 48GB | ~0.5 tok/s | 고품질, 저속 허용 시 |

```
권장 최소 사양 (Ollama CPU):
  CPU:    8코어 이상 (코어 수가 속도에 직결)
  RAM:    32GB (Gemma2:9b 기준, 여유 포함)
  디스크: 30GB (모델 파일)
```

---

### LLM 서빙 — vLLM (GPU 필수)

| GPU | VRAM | 구동 모델 | 처리량 (TPS) |
|-----|------|-----------|--------------|
| RTX 3090 | 24GB | Gemma2:9b | ~60 tok/s |
| RTX 4090 | 24GB | Gemma2:9b | ~100 tok/s |
| A100 40GB | 40GB | Gemma2:27b | ~80 tok/s |
| A100 80GB | 80GB | Gemma2:27b (FP16) | ~150 tok/s |

```
권장 최소 사양 (vLLM):
  GPU:    RTX 3090 이상 (VRAM 24GB)
  CPU:    8코어
  RAM:    32GB
  디스크: 50GB SSD
  CUDA:   12.1 이상

GPU 서버 없는 경우:
  RunPod, Lambda Labs 등 클라우드 GPU 시간 단위 임대
  비용: 약 $0.5~2/시간
```

---

### Embedding 모델 (ko-sroberta)

| 항목 | 최소 | 권장 | 비고 |
|------|------|------|------|
| CPU | 2코어 | 4코어 | |
| RAM | 2GB | 4GB | 모델 로딩 약 1.2GB |
| GPU | 불필요 | RTX 3060 이상 | GPU 있으면 약 10배 빠름 |

```
인덱싱 속도 비교 (1,000 청크 기준):
  CPU만 사용:          약 3~5분
  GPU (RTX 3060):      약 20초
  → 초기 인덱싱 1회성 작업이므로 CPU도 충분
```

---

### Prometheus + Grafana

| 항목 | 최소 | 권장 |
|------|------|------|
| CPU | 1코어 | 2코어 |
| RAM | 2GB | 4GB |
| 디스크 | 20GB | 50GB |

```
Prometheus TSDB 예상 크기:
  메트릭 수집 15초 간격, 90일 보존 기준: 약 10~20GB
```

---

## 3. 도입 시나리오별 서버 구성

### 시나리오 1 — 내부 PoC (개념 검증)

```
목적:    기능 동작 확인, 도입 가능성 검토
기간:    1~2개월
LLM:     Anthropic API (AIRGAP_MODE=false)

하드웨어:
  기존 개발 PC 또는 사무용 서버 활용
  RAM 32GB 이상이면 충분

비용:
  추가 하드웨어: 없음
  Anthropic API: 약 5~10만원/월 예상
```

---

### 시나리오 2 — 사내 소규모 서비스

```
목적:    실 사용자 대상 서비스 운영
대상:    동시 사용자 5~10명
LLM:     Ollama + Gemma2:9b (CPU)

서버 1대 구성:
  CPU:    16코어 (Intel Xeon or AMD EPYC)
  RAM:    64GB ECC
  디스크: 1TB NVMe SSD
  네트워크: 1Gbps

예상 비용:
  신품 서버: 약 200~300만원
  중고 서버: 약 100~150만원
```

---

### 시나리오 3 — 고객사 납품 (프로덕션, 폐쇄망)

```
목적:    고객사 폐쇄망 납품
대상:    동시 사용자 20~50명
LLM:     vLLM + Gemma2:9b (GPU)
모드:    AIRGAP_MODE=true

서버 1 — LLM 서빙 전용:
  CPU:    8코어
  RAM:    32GB
  GPU:    RTX 4090 (24GB VRAM)
  디스크: 500GB SSD
  예산:   약 250만원 (RTX 4090 기준)

서버 2 — 앱 + DB + Kafka:
  CPU:    16코어
  RAM:    64GB
  디스크: 1TB NVMe SSD
  예산:   약 150~200만원

합산 예산: 약 400~500만원
```

---

## 4. 전체 스토리지 요약

```
구성 요소별 예상 디스크 사용량:
  PostgreSQL:          50GB
  Qdrant:              10GB
  Neo4j:                5GB
  Kafka (30일 보존):  100GB
  Prometheus (90일):   20GB
  LLM 모델 파일:       20GB
  Embedding 모델:       2GB
  앱 로그:             10GB
  여유 공간 (30%):     65GB
  ─────────────────────────
  합계:               ~280GB

권장 구성: 500GB NVMe SSD
```

---

## 5. LLM 선택 흐름

```
완전 폐쇄망 (에어갭)?
    │
    ├─ Yes → GPU 서버 보유?
    │           ├─ Yes → vLLM + Gemma2:9b  (권장)
    │           └─ No  → Ollama + Gemma2:9b (CPU, 느림)
    │
    └─ No → AWS 사용 고객사?
                ├─ Yes → AWS Bedrock (Claude, VPC 격리)
                └─ No  → Anthropic API (Claude, 가장 빠른 시작)
```

---

## 6. 운영 체제 권장 사양

```
권장 OS: Ubuntu 22.04 LTS (서버 에디션)
  - Docker Engine 24.0+
  - Docker Compose v2
  - kubectl 1.28+ (K8s 환경 시)

GPU 환경 추가 설치:
  - NVIDIA Driver 535+
  - CUDA 12.1+
  - nvidia-container-toolkit
```
